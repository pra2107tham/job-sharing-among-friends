node_modules/
dist/
build/
.turbo/
*.tsbuildinfo

# expo
.expo/
web-build/
# Prebuild output only. These MUST stay anchored with a leading slash: bare
# `ios/` and `android/` match any directory with that name at any depth, which
# silently excluded every Kotlin and Swift source in
# apps/mobile/modules/jobdrop-share/. The M3 native code was written, passed
# review, and was not in the repository.
/apps/mobile/android/
/apps/mobile/ios/
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# env
.env
.env.*
!.env.example

# local postgres cluster + logs (see scripts/db.sh)
.pgdata/
*.log

# os / editor
.DS_Store
.idea/
.vscode/
